<Window x:Class="GitBook.MainWindow"
   xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
   xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
   xmlns:gitBook="clr-namespace:GitBook"
   AllowsTransparency="True"
   Background="Transparent"
   Title="MainWindow"
   Width="960"
   Height="200"
   WindowStartupLocation="CenterScreen"
   ResizeMode="NoResize"
   WindowStyle="None"
   FocusManager.FocusedElement="{Binding ElementName=CommitText}">
   <Border CornerRadius="14" Padding="10" Background="#8000">
      <Border CornerRadius="10" Padding="12" Background="#A69864">
         <StackPanel>
            <TextBlock Margin="10,0,0,0"
               Foreground="#FFF"
               VerticalAlignment="Center"
               Text="Committing" />

            <TextBox x:Name="CommitText"
               Background="#CBC5AF"
               Foreground="#FFFEF9"
               FontFamily="Consolas"
               FontSize="24"
               MaxLength="72">
            </TextBox>

            <TextBox x:Name="SecondaryCommitText"
               Background="#CBC5AF"
               Foreground="#FFFEF9"
               FontFamily="Consolas"
               FontSize="24"
               MaxLength="72">
            </TextBox>
         </StackPanel>
      </Border>
   </Border>
</Window>
