<Window x:Class="GitBook.MainWindow"
   xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
   xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
   xmlns:gitBook="clr-namespace:GitBook"
   AllowsTransparency="True"
   Background="Transparent"
   Title="MainWindow"
   Width="960"
   Height="200"
   WindowStartupLocation="CenterScreen"
   ResizeMode="NoResize"
   WindowStyle="None"
   FocusManager.FocusedElement="{Binding ElementName=CommitText}">
   <Border CornerRadius="16" Padding="20" Background="Blue">
      <StackPanel>
         <Grid Background="#666">
            <TextBlock Margin="10,0,0,0"
            Foreground="#FFF"
            VerticalAlignment="Center"
            Text="Committing" />
         </Grid>

         <TextBox x:Name="CommitText"
            Background="#054263"
            Foreground="#E6CA7E"
            FontFamily="Consolas"
            FontSize="24"
            MaxLength="72">
         </TextBox>

         <TextBox x:Name="SecondaryCommitText"
            Background="#054263"
            Foreground="#E6CA7E"
            FontFamily="Consolas"
            FontSize="24"
            MaxLength="72">
         </TextBox>
      </StackPanel>
   </Border>
</Window>
