<views:WindowBase x:Class="GitWrite.Views.InteractiveRebaseWindow"
   x:Name="PageRoot"
   xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
   xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
   xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
   xmlns:cmd="clr-namespace:GalaSoft.MvvmLight.Command;assembly=GalaSoft.MvvmLight.Extras.WPF45"
   xmlns:views="clr-namespace:GitWrite.Views"
   xmlns:behaviors="clr-namespace:GitWrite.Behaviors"
   xmlns:controls="clr-namespace:GitWrite.Views.Controls"
   AllowsTransparency="True"
   Background="Transparent"
   Title="CommitWindow"
   Width="1036"
   Height="500"
   ResizeMode="NoResize"
   WindowStyle="None"
   FocusManager.FocusedElement="{Binding ElementName=RebasePanel}"
   DataContext="{Binding InteractiveRebaseViewMdoel, Source={StaticResource ViewModelLocator}}">

   <Window.Resources>
      <ResourceDictionary>
         <ResourceDictionary.MergedDictionaries>
            <ResourceDictionary Source="..\Styles\TextBoxStyle.xaml" />
            <ResourceDictionary Source="..\Styles\ButtonStyle.xaml" />
            <ResourceDictionary Source="CommitWindowResources.xaml" />
         </ResourceDictionary.MergedDictionaries>
      </ResourceDictionary>
   </Window.Resources>

   <Grid x:Name="MainGrid">
      <Border x:Name="OuterBorder"
         CornerRadius="14"
         Background="{StaticResource WindowBackgroundColor}"
         BorderThickness="2"
         BorderBrush="{StaticResource WindowBorderColor}">
         <Grid>
            <Grid.RenderTransform>
               <TranslateTransform x:Name="OuterBorderTranslateTransform" />
            </Grid.RenderTransform>

            <Border x:Name="InnerBorder"
               CornerRadius="8"
               Padding="16"
               VerticalAlignment="Stretch">
               <Grid VerticalAlignment="Stretch">
                  <controls:OldInteractiveRebasePanel x:Name="RebasePanel"
                     ItemsSource="{Binding Items}">
                  </controls:OldInteractiveRebasePanel>
               </Grid>
            </Border>
         </Grid>
      </Border>
   </Grid>
</views:WindowBase>
